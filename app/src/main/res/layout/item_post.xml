<?xml version="1.0" encoding="utf-8"?>
<!--
    LAYOUT: item_post.xml
    DESCRIPCIÓN: Diseño individual para cada publicación en el feed.
    ESTRUCTURA:
      - CardView: Contenedor principal con bordes redondeados y sombra.
      - LinearLayout (Vertical):
          - TextView (Autor): Nombre del usuario que publicó.
          - TextView (Fecha): Tiempo transcurrido desde la publicación.
          - TextView (Contenido): Texto del post.
          - ImageView (Imagen): Foto adjunta (visible solo si existe).
          - Button (Comentar): Botón para ver/agregar comentarios.
-->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Nombre del Autor -->
        <TextView
            android:id="@+id/tvAutorPost"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Autor"
            android:textStyle="bold"
            android:textSize="16sp"
            android:textColor="#000000"/>

        <!-- Fecha relativa (e.g., "Hace 5 min") -->
        <TextView
            android:id="@+id/tvFechaPost"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Fecha"
            android:textSize="12sp"
            android:textColor="#757575"/>

        <!-- Cuerpo del texto de la publicación -->
        <TextView
            android:id="@+id/tvContenidoPost"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Contenido"
            android:textSize="14sp"
            android:textColor="#333333"
            android:layout_marginTop="8dp"/>

        <!-- Imagen adjunta (Opcional, visibilidad controlada por código) -->
        <ImageView
            android:id="@+id/imgPost"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginTop="8dp"
            android:scaleType="centerCrop"
            android:visibility="gone"
            android:background="#F0F0F0"
            android:src="@android:drawable/ic_menu_gallery" />

        <!-- Botón de acción para ir a comentarios -->
        <Button
            android:id="@+id/btnComentar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Comentar"
            android:layout_marginTop="10dp"/>

    </LinearLayout>
</androidx.cardview.widget.CardView>
